#!/bin/bash

packageName="venus-onecontrol-can"
source "/data/SetupHelper/HelperResources/IncludeHelpers"

# No pip/opkg dependencies -- everything in src/ is Python stdlib only
# (see ARCHITECTURE.md's "Platform Constraints (Venus OS)" section). Nothing
# to do on INSTALL/UNINSTALL beyond the standard file/service handling below.

endScript 'INSTALL_FILES' 'INSTALL_SERVICES'
